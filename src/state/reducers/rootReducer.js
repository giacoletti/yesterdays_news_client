const rootReducer = (state, action) => {
  switch (action.type) {
    case 'SET_LANGUAGE':
      // return {
      //   ...state,
        
      // }
    default:
      return state;
  }
};

export default rootReducer;
